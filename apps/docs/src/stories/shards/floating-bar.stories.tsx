import { Meta, StoryObj } from '@storybook/react'
import { FloatingNav } from '../../components/floating-nav'

export default {
  title: 'Shards/floating-bar',
  component: FloatingNav,
} as Meta

export const Default: StoryObj = {}
