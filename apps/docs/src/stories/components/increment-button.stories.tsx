import { Meta, StoryObj } from '@storybook/react'
import { IncrementButton } from '@sw-ec/ui'

export default {
  title: 'components/increment-button',
  component: IncrementButton,
} as Meta
export const Default: StoryObj = {}
